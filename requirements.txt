# Python >= 3.10 is required (the code uses PEP 604 `X | None` type unions).
# Install PyTorch matching your CUDA version first: https://pytorch.org/get-started/locally/
torch>=2.0
torchvision>=0.15
timm>=0.9.0
monai>=1.3.0
numpy>=1.24
scipy>=1.10
h5py>=3.8
Pillow>=9.5
albumentations>=1.3.0
ptflops>=0.7
tqdm>=4.65
PyYAML>=6.0
yacs>=0.1.8
medpy>=0.4.0   # only needed for the (optional) HD95 metric in val.py
